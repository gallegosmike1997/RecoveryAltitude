import { RecoveryLandingPage } from "@/components/recovery-altitude/RecoveryLandingPage";

export default function FieldRegisterPage() {
  return (
    <RecoveryLandingPage
      description="A durable route register for repeat visitors who want to scan the full Recovery Altitude offer at a glance."
      title="Keep the next step visible."
      variant="field-register"
    />
  );
}
