import { RecoveryLandingPage } from "@/components/recovery-altitude/RecoveryLandingPage";

export default function TrailheadPage() {
  return (
    <RecoveryLandingPage
      description="A calm, service-first entry point for outdoor recovery and altitude wellness."
      title="A clearer route back to the outdoors."
      variant="trailhead"
    />
  );
}
