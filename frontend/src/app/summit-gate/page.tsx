import { RecoveryLandingPage } from "@/components/recovery-altitude/RecoveryLandingPage";

export default function SummitGatePage() {
  return (
    <RecoveryLandingPage
      description="A grounded masthead for people who want context, confidence, and a practical next conversation."
      title="Find the right starting point before the next ascent."
      variant="summit-gate"
    />
  );
}
